<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingLeft="12dip"
    android:paddingRight="12dip" >

    <!-- android:background="@drawable/ppsgame_menu_item_bg" -->

    <RelativeLayout
        android:id="@+id/icon"
        android:layout_width="22dip"
        android:layout_height="22dip"
        android:layout_centerHorizontal="true" >

        <ImageView
            android:id="@+id/ppsgame_item_img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_alignParentBottom="true"
            android:src="@drawable/slidebar_ppsgame_menu_cs" />

        <ImageView
            android:id="@+id/ppsgame_item_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toRightOf="@id/ppsgame_item_img"
            android:src="@drawable/sliderbar_menu_item_forum"
            android:visibility="gone" />
    </RelativeLayout>

    <TextView
        android:id="@+id/ppsgame_item_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/icon"
        android:layout_centerHorizontal="true"
        android:paddingBottom="3dip"
        android:layout_marginTop="3dp"
        android:text="@string/sliderbar_huodong"
        android:textColor="#5d4640"
        android:textSize="13sp" />

</RelativeLayout>