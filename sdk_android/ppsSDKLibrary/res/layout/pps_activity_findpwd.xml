<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/ppsgame_bg" >

    <RelativeLayout
        android:id="@+id/findpwd_title"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:background="@drawable/ppsgame_top_bg"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/findpwd_back"
            style="@style/ppsgame_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/pps_findTitle"
            android:textColor="@color/button_color"
            android:textSize="20.0sp" />
    </RelativeLayout>

    <WebView
        android:id="@+id/findpwd_webview"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/findpwd_title" />

</RelativeLayout>